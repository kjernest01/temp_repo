import React from "react";

export const SearchResultList = () => {
    return (
        <div className="results-list">
            {/* results.map((result, id) => {

            }) */}
        </div>
    )
};